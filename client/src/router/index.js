import {
  createBrowserRouter,
  
  Route,
  Link,
  HashRouter,
} from "react-router-dom";

const Routes = () => {


return(
<HashRouter>
    <Routes>
    <Route index element={<Acceuil />} />
    <Route index element={<Acceuil />} />
    <Route index element={<Acceuil />} />
    <Route index element={<Acceuil />} />

    </Routes>
</HashRouter>
)

};

export default Routes;
