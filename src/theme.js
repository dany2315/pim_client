import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#82aff9",
      
      dark:'#8eb8fb',
    },
    secondary: {
      main: '#b26500',
    },
  },
});

export default theme